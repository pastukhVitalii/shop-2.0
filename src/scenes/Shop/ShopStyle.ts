import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  card: {
    backgroundColor: 'blue',
    margin: '30px',
    width: '300px',
    height: '400px',
  },
});
