import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  root: {
    maxWidth: 345,
  },
  media: {
    width: '60%',
    margin: '0 auto',
    height: '170px',
  },
});